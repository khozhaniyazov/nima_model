---
phase: 02-quality-reliability
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - algorithms/ai_functions.py
  - algorithms/template_registry.py
  - algorithms/plan/compiler.py
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-03
  - DOM-04

must_haves:
  truths:
    - "Math domain uses deterministic plan compiler for FAST_PIPELINE"
    - "Physics domain has arrow/field visualization patterns"
    - "CS domain has array/tree/graph animation patterns"
    - "Chemistry domain has molecule/reaction animation patterns"
  artifacts:
    - path: "algorithms/ai_functions.py"
      provides: "Enhanced domain-specific guidance"
      contains: "get_domain_specific_guidance"
    - path: "algorithms/template_registry.py"
      provides: "Domain-specific template selection"
      contains: "choose_template"
    - path: "algorithms/plan/compiler.py"
      provides: "Deterministic plan compilation"
      contains: "compile_plan"
---

<objective>
Improve domain-specific animation generation for all 4 domains (math, physics, CS, chemistry). Ensure deterministic plan compiler works for math, and add/improve patterns for physics, CS, and chemistry domains. Addresses DOM-01, DOM-02, DOM-03, DOM-04 requirements.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@algorithms/ai_functions.py
@algorithms/template_registry.py
@algorithms/plan/compiler.py

**Phase requirement IDs:** DOM-01, DOM-02, DOM-03, DOM-04

**Project context:**
- NIMA uses domain-specific guidance prompts injected into generation
- Deterministic plan compiler exists for math FAST_PIPELINE but template coverage is limited
- Domain guidance exists but lacks depth for physics, CS, and chemistry
- RAG system retrieves patterns by domain/topic matching
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand deterministic plan compiler templates for math domain</name>
  <files>algorithms/template_registry.py, algorithms/plan/compiler.py</files>
  <read_first>
    - algorithms/template_registry.py
    - algorithms/plan/compiler.py
  </read_first>
  <action>
    Expand template coverage in algorithms/template_registry.py and enhance compiler in algorithms/plan/compiler.py:

1. **Add new math templates to TEMPLATES dict**:
   - `derivative_exploration` — axes + graph + tangent line animation with dRiemann rectangles
   - `sequence_convergence` — number line + dot animation showing convergence
   - `matrix_transform` — NumberPlane + unit square + linear transformation with basis vectors
   - `area_under_curve` — axes + graph + shaded region with Riemann sum animation
   - `step_by_step_proof` — TransformMatchingTex equation transformations

2. **Enhance compiler.py object types**:
   - Add support for Arrow objects (physics vectors)
   - Add support for Circle+Text nodes (CS trees)
   - Add support for Dot+Line molecule representations (chemistry)
   - Improve VGroup handling for composite objects

3. **Improve template selection logic** in choose_template():
   - Add keyword matching for new templates
   - Ensure DOM-01 math domain maps to deterministic compiler path

Current templates: two_panel_comparison, definition_to_example, step_by_step_derivation, graph_and_formula, mapping_diagram

Add: derivative_exploration, sequence_convergence, matrix_transform, area_under_curve, step_by_step_proof
  </action>
  <acceptance_criteria>
    - grep "derivative_exploration" template_registry.py returns matches
    - grep "sequence_convergence" template_registry.py returns matches
    - grep "matrix_transform" template_registry.py returns matches
    - compile_plan handles Arrow, Circle, Dot, Line objects
    - Math domain FAST_PIPELINE uses deterministic compiler
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.template_registry import choose_template; t = choose_template('explain derivative limit', 'math'); print(t)"</automated>
  </verify>
  <done>Math domain has expanded template coverage and deterministic compilation</done>
</task>

<task type="auto">
  <name>Task 2: Enhance physics domain guidance and patterns</name>
  <files>algorithms/ai_functions.py</files>
  <read_first>
    - algorithms/ai_functions.py (get_domain_specific_guidance function)
    - RAG/RAG_system.py (CORPUS for physics patterns)
  </read_first>
  <action>
    Enhance physics domain support in algorithms/ai_functions.py and add physics patterns to RAG corpus:

1. **Expand physics domain guidance** in get_domain_specific_guidance()["physics"]:
   - Add ArrowVectorField pattern for electric/magnetic fields
   - Add StreamLines for flow visualizations
   - Add curved Arrow for rotational motion (torque, angular momentum)
   - Add pendulum/spring oscillation patterns using ValueTracker + always_redraw
   - Add wave superposition patterns (two waves merging)

2. **Add physics RAG patterns** to RAG/RAG_system.py CORPUS:
   - Vector field pattern (ArrowVectorField usage)
   - Pendulum oscillation pattern
   - Wave superposition pattern
   - Force diagram pattern (multiple arrows with labels)

3. **Update physics guidance text** to include:
   - "Use Arrow(length= magnitude, buff=0) for force vectors"
   - "For fields: ArrowVectorField for discrete, StreamLines for continuous"
   - "Label vectors with MathTex showing magnitude and direction"
   - "Use color to encode physical quantity (e.g., kinetic energy: BLUE→RED)"
  </action>
  <acceptance_criteria>
    - grep "ArrowVectorField" ai_functions.py returns physics guidance matches
    - grep "pendulum" RAG_system.py has physics pattern
    - grep "physics" ai_functions.py includes field visualization guidance
    - Physics domain generation produces appropriate arrow/field visuals
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.ai_functions import get_domain_specific_guidance; g = get_domain_specific_guidance('physics'); print('ArrowVectorField' in g or 'StreamLines' in g)"</automated>
  </verify>
  <done>Physics domain has comprehensive guidance for arrows, fields, and oscillations</done>
</task>

<task type="auto">
  <name>Task 3: Add CS domain patterns (arrays, trees, graphs)</name>
  <files>algorithms/ai_functions.py, RAG/RAG_system.py</files>
  <read_first>
    - algorithms/ai_functions.py (get_domain_specific_guidance function)
    - RAG/RAG_system.py (CORPUS structure)
  </read_first>
  <action>
    Add comprehensive CS domain animation patterns:

1. **Enhance CS domain guidance** in get_domain_specific_guidance()["computer_science"]:
   - Add detailed array visualization patterns ( Rectangle+Text VGroups, indices below)
   - Add tree node visualization (Circle with Text inside, Lines for edges)
   - Add graph visualization (Dot nodes, Line edges, adjacency representation)
   - Add sorting algorithm animation patterns (bubble sort, quicksort steps)
   - Add linked list node+arrow pattern

2. **Add CS RAG patterns** to RAG/RAG_system.py CORPUS:
   - Array sorting pattern (color swaps, comparisons)
   - Binary tree traversal pattern (level-order with highlight)
   - Graph BFS/DFS pattern (visited set visualization)
   - Linked list insertion pattern
   - Stack/queue visualization pattern

3. **CS guidance should specify**:
   - "Array elements: VGroup(Rectangle(width=0.8, height=0.6), Text(str(val))).arrange(DOWN)"
   - "Tree nodes: Circle(radius=0.4) containing Text label, connected by Line"
   - "Use Play(Indicate(node)) to highlight current element"
   - "Show swap operations with ReplacementTransform, not direct assignment animation"
  </action>
  <acceptance_criteria>
    - grep "array" ai_functions.py includes VGroup rectangle pattern
    - grep "tree" ai_functions.py includes Circle+Text node pattern
    - grep "sorting" RAG_system.py has CS sorting pattern
    - grep "computer_science" RAG_system.py CORPUS has multiple CS entries
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.ai_functions import get_domain_specific_guidance; g = get_domain_specific_guidance('computer_science'); print('VGroup' in g and 'Circle' in g)"</automated>
  </verify>
  <done>CS domain has array, tree, graph, and sorting animation patterns</done>
</task>

<task type="auto">
  <name>Task 4: Add chemistry domain patterns (molecules, reactions)</name>
  <files>algorithms/ai_functions.py, RAG/RAG_system.py</files>
  <read_first>
    - algorithms/ai_functions.py (get_domain_specific_guidance function)
    - RAG/RAG_system.py (CORPUS structure)
  </read_first>
  <action>
    Add chemistry domain animation patterns:

1. **Enhance chemistry domain guidance** in get_domain_specific_guidance()["chemistry"]:
   - Add CPK coloring reference (H=WHITE, C=GREY, O=RED, N=BLUE, S=YELLOW, Cl=GREEN, P=ORANGE)
   - Add molecule building pattern (Dot for atoms, Line for bonds)
   - Add reaction mechanism animation pattern (curved Arrow for electron movement)
   - Add electron cloud/shell visualization pattern
   - Add periodic table group highlighting pattern

2. **Add chemistry RAG patterns** to RAG/RAG_system.py CORPUS:
   - Water molecule pattern (H2O with correct angles)
   - Organic reaction pattern (ethane formation, substitution)
   - Enzyme-substrate interaction pattern
   - Electron orbital pattern
   - Balancing chemical equation pattern

3. **Chemistry guidance should specify**:
   - "Atoms: Dot(radius=0.3) with MathTex label nearby"
   - "Bonds: Line with optional SingleBond/DoubleBond/TripleBond"
   - "Use CurvedArrow for electron movement in mechanisms"
   - "Reactants on LEFT, arrow in CENTER, products on RIGHT"
   - "Show stereochemistry with wedge/dash notation where relevant"
  </action>
  <acceptance_criteria>
    - grep "CPK" ai_functions.py includes color reference
    - grep "molecule" ai_functions.py includes Dot+Line building pattern
    - grep "reaction" RAG_system.py has chemistry reaction pattern
    - grep "chemistry" RAG_system.py CORPUS has multiple chemistry entries
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.ai_functions import get_domain_specific_guidance; g = get_domain_specific_guidance('chemistry'); print('Dot' in g and 'Line' in g)"</automated>
  </verify>
  <done>Chemistry domain has molecule, reaction, and bond visualization patterns</done>
</task>

</tasks>

<verification>
- Test each domain with a sample prompt to verify guidance is injected
- Verify deterministic compiler activates for math + FAST_PIPELINE
- Check that RAG patterns exist for all 4 domains
- Verify template_registry chooses appropriate template per domain
</verification>

<success_criteria>
- Math domain uses deterministic plan compiler for FAST_PIPELINE (DOM-01)
- Physics domain produces arrow/field visualizations (DOM-02)
- CS domain produces array/tree/graph animations (DOM-03)
- Chemistry domain produces molecule/reaction animations (DOM-04)
- All 4 domains have RAG corpus patterns
- Domain guidance is comprehensive and domain-appropriate
</success_criteria>

<output>
After completion, update `.planning/phases/02-quality-reliability/02-01-PLAN-SUMMARY.md` to reflect combined progress
</output>
