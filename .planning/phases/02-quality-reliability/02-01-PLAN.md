---
phase: 02-quality-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - algorithms/overlap_detector.py
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - LAY-03

must_haves:
  truths:
    - "Position collisions are detected before rendering"
    - "Object accumulation issues are caught"
    - "Section cleanup helpers are validated"
  artifacts:
    - path: "algorithms/overlap_detector.py"
      provides: "Pre-render layout validation"
      contains: "detect_position_collisions"
      min_lines: 150
    - path: "algorithms/overlap_detector.py"
      provides: "Section lifecycle validation"
      contains: "detect_missing_section_cleanup"
  key_links:
    - from: "app.py"
      to: "overlap_detector.py"
      via: "run_all_checks() called in validation pipeline"
      pattern: "run_all_checks"
---

<objective>
Improve overlap detector with comprehensive checks for position collisions, object accumulation, and section lifecycle validation. Addresses LAY-01, LAY-02, LAY-03 requirements.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@algorithms/overlap_detector.py

**Phase requirement IDs:** LAY-01, LAY-02, LAY-03

**Project context:**
- NIMA uses static AST+regex analysis BEFORE rendering to catch layout problems
- Overlap detector runs in generate_and_validate_code() pipeline
- Current detector has basic checks but needs edge case handling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance position collision detection</name>
  <files>algorithms/overlap_detector.py</files>
  <read_first>
    - algorithms/overlap_detector.py (existing implementation)
  </read_first>
  <action>
    Enhance `detect_position_collisions()` in algorithms/overlap_detector.py:

1. **Add edge position normalization** - Currently `_normalize_pos()` doesn't handle edge positions like UP, DOWN, LEFT, RIGHT, ORIGIN properly. Add:
   - Pattern for `\.to_edge\(UP\)` → normalize to edge:UP
   - Pattern for `\.to_edge\(DOWN\)` → normalize to edge:DOWN
   - Pattern for `\.to_edge\(LEFT\)` → normalize to edge:LEFT
   - Pattern for `\.to_edge\(RIGHT\)` → normalize to edge:RIGHT
   - Handle combinations: UP+LEFT, DOWN+RIGHT, etc.

2. **Add VGroup child tracking** - When a VGroup is placed, track its children separately:
   - Extract child positions from VGroup placement
   - Check children for collisions with other objects

3. **Improve between-line analysis** - Current check looks for FadeOut between placements but misses:
   - Transform that removes var_a
   - self.remove() calls
   - animate methods that change visibility

4. **Add coordinate tuple handling** - Handle `np.array([x, y, z])` and `(x, y, z)` position formats

The existing function signature: `detect_position_collisions(code: str) -> List[str]`
Add new helper: `_normalize_edge_pos(expr: str) -> str`
  </action>
  <acceptance_criteria>
    - grep "edge:UP" overlap_detector.py returns matches
    - grep "VGroup" overlap_detector.py has child position tracking
    - detect_position_collisions handles np.array positions
    - detect_position_collisions handles edge positions (UP, DOWN, LEFT, RIGHT)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.overlap_detector import detect_position_collisions; code='o1.to_edge(UP)\no2.to_edge(UP)'; print(detect_position_collisions(code))"</automated>
  </verify>
  <done>Position collision detection catches edge-to-edge overlaps, VGroup children, and tuple positions</done>
</task>

<task type="auto">
  <name>Task 2: Improve section lifecycle validation</name>
  <files>algorithms/overlap_detector.py</files>
  <read_first>
    - algorithms/overlap_detector.py
  </read_first>
  <action>
    Enhance `detect_missing_section_cleanup()` and add new checks in algorithms/overlap_detector.py:

1. **Detect start_section/end_section usage patterns** - Current check only looks for helpers_present. Improve to:
   - Verify end_section() is called between start_section() calls
   - Check that section groups are properly added to returned groups
   - Detect nested start_section without end_section

2. **Add section object tracking** - Track objects added to section groups:
   - Objects added via `section.add()` should be tracked
   - When next section starts, check if previous section objects are cleaned up

3. **Improve the "NO_CLEANUP" warning** - Current window is 8 lines. Increase to 15 lines and add:
   - Check for `*self.mobjects` cleanup patterns
   - Check for `self.remove()` calls
   - Detect if all mobjects are cleared via `FadeOut(*self.mobjects)`

4. **Add new function `detect_section_leak()`** - Detect objects that persist across section boundaries without being in a section group:
   ```python
   def detect_section_leak(code: str) -> List[str]:
       """Detect objects created in one section that appear in the next without cleanup."""
       # Implementation needed
   ```

The existing function `detect_missing_section_cleanup()` should be enhanced.
  </action>
  <acceptance_criteria>
    - grep "detect_section_leak" overlap_detector.py returns matches
    - grep "section.add" overlap_detector.py has tracking logic
    - detect_missing_section_cleanup handles 15-line windows
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.overlap_detector import detect_missing_section_cleanup, detect_section_leak; print('Functions exist')"</automated>
  </verify>
  <done>Section lifecycle validation detects leaks and improper helper usage</done>
</task>

<task type="auto">
  <name>Task 3: Add object accumulation heuristics</name>
  <files>algorithms/overlap_detector.py</files>
  <read_first>
    - algorithms/overlap_detector.py
  </read_first>
  <action>
    Enhance `detect_object_accumulation()` in algorithms/overlap_detector.py:

1. **Improve cleanup ratio calculation** - Current threshold is 40% (creates > 10 and cleanup < 40%). Adjust:
   - Consider object types: NumberPlane creates many mobjects internally
   - VGroup children should count individually
   - MathTex creates multiple sub-mobjects

2. **Add size-based heuristics** - Large objects (NumberPlane, ComplexPlane) should weight more:
   - NumberPlane counts as 5 objects
   - ComplexPlane counts as 5 objects
   - Regular MathTex counts as 3 objects (has parts)

3. **Detect animation queue buildup** - Check for patterns like:
   - Many successive Create/Write without waits
   - Multiple animations on same object in sequence

4. **Improve helper detection** - Current check `helpers_used = len(re.findall(r'start_section\(self', code)) >= 2` is too simple:
   - Check that start_section is called AND end_section is called
   - Verify section return value is used

Add patterns:
```python
# Detect section helper usage
start_count = len(re.findall(r'start_section\(self', code))
end_count = len(re.findall(r'end_section\(', code))
if start_count > 0 and end_count == 0:
    warnings.append("[SECTION_HELPERS_UNUSED] start_section called but no end_section")
```
  </action>
  <acceptance_criteria>
    - detect_object_accumulation handles weighted object counts
    - grep "NumberPlane" overlap_detector.py has weighting logic
    - detect_object_accumulation warns on missing end_section
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.overlap_detector import detect_object_accumulation; code='n=NumberPlane()'; print(detect_object_accumulation(code))"</automated>
  </verify>
  <done>Object accumulation detection uses size-based heuristics and proper helper tracking</done>
</task>

</tasks>

<verification>
- Run: `python -c "from algorithms.overlap_detector import run_all_checks; print(run_all_checks(open('algorithms/overlap_detector.py').read()))"`
- Verify all 5 detection functions are called in run_all_checks()
- Check that app.py imports and uses run_all_checks() properly
</verification>

<success_criteria>
- Overlap detector catches edge position collisions (LAY-01)
- Overlap detector catches VGroup child collisions (LAY-01)
- Object accumulation detection uses size-based heuristics (LAY-02)
- Section lifecycle validation detects leaks and missing end_section (LAY-03)
- All functions have proper docstrings
</success_criteria>

<output>
After completion, create `.planning/phases/02-quality-reliability/02-01-PLAN-SUMMARY.md`
</output>
