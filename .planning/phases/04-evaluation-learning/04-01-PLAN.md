---
phase: 04-evaluation-learning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - algorithms/ai_functions.py
  - app.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03

must_haves:
  truths:
    - "Every successful render produces a quality score"
    - "Scores are visible via /stats endpoint"
    - "High-quality examples are retrievable for RAG"
    - "Error patterns are tracked and inform generation"
  artifacts:
    - path: "algorithms/ai_functions.py"
      provides: "Quality evaluation scoring"
      contains: "evaluate_with_gpt4"
    - path: "app.py"
      provides: "Stats endpoint and score storage"
      contains: "/stats"
    - path: "app.py"
      provides: "High-scorer retrieval"
      contains: "get_best_examples"

must_verifs:
  - verify_eval_runs: "Evaluation runs on successful renders"
  - verify_stats_endpoint: "/stats returns quality metrics"
  - verify_high_scorers: "High-quality examples (≥80) retrievable for RAG"
---

<objective>
Ensure evaluation runs reliably on all successful renders, enhance /stats endpoint with better metrics, and verify high-scorer retrieval works for RAG. Addresses EVAL-01, EVAL-02, EVAL-03 requirements.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@algorithms/ai_functions.py
@app.py

**Phase requirement IDs:** EVAL-01, EVAL-02, EVAL-03

**Project context:**
- evaluate_with_gpt4 exists but may not run reliably on all successful renders
- /stats endpoint is basic, needs improvement
- High-scorer retrieval for RAG depends on quality scores being stored
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure evaluation runs reliably on all successful renders</name>
  <files>app.py</files>
  <read_first>
    - app.py (save_and_render function around evaluation call)
  </read_first>
  <action>
    Verify and fix evaluation trigger on successful renders:

1. **Check evaluation call in save_and_render()**:
   - Find where evaluate_with_gpt4 is called
   - Verify it's called for all successful renders (not just non-FAST mode)
   - Ensure evaluation runs even when FAST_PIPELINE is enabled

2. **Check evaluation storage**:
   - verify save_ai_evaluation is called after successful render
   - ensure errors in evaluation don't crash the render pipeline
   - evaluation should be non-blocking

3. **Fix any gaps**:
   - If evaluation is conditional, remove condition for successful renders
   - Add try/except wrapper to prevent evaluation errors from affecting user
   - Ensure evaluation happens after video is confirmed to exist
  </action>
  <acceptance_criteria>
    - evaluate_with_gpt4 called for all successful renders
    - Evaluation errors don't crash pipeline
    - Scores are saved to database
  </acceptance_criteria>
  <verify>
    <automated>grep -A5 "evaluate_with_gpt4" app.py | head -20</automated>
  </verify>
  <done>Evaluation runs reliably on every successful render</done>
</task>

<task type="auto">
  <name>Task 2: Enhance /stats endpoint with better metrics</name>
  <files>app.py</files>
  <read_first>
    - app.py (stats function around line 1276)
  </read_first>
  <action>
    Enhance /stats endpoint with richer metrics:

1. **Current stats returned**:
   - total_requests, successful_renders, avg_quality_score, unique_error_patterns
   - top_domains (basic)

2. **Add more useful metrics**:
   - Success rate: successful_renders / total_requests
   - Quality distribution: % above 80, % above 70, % below 60
   - Average scores per dimension (layout, educational, technical, pacing, manim)
   - Most common error patterns
   - Average render duration
   - Renders per day/week trends (if data available)

3. **Add endpoint**:
   - GET /stats/quality - detailed quality breakdown
   - GET /stats/domains - domain-specific stats
   - GET /stats/errors - error pattern summary

4. **Improve response format**:
   - Include success_rate percentage
   - Include quality_tiers breakdown
   - Include timestamp of last render

Current query is basic - enhance with aggregations.
  </action>
  <acceptance_criteria>
    - /stats includes success_rate
    - /stats includes quality distribution
    - /stats includes error pattern summary
    - Response is well-formatted JSON
  </acceptance_criteria>
  <verify>
    <automated>grep -c "success_rate\|quality_tiers" app.py</automated>
  </verify>
  <done>/stats endpoint shows comprehensive quality metrics</done>
</task>

<task type="auto">
  <name>Task 3: Verify high-scorer retrieval for RAG</name>
  <files>app.py, RAG/RAG_system.py</files>
  <read_first>
    - app.py (get_best_examples function)
    - RAG/RAG_system.py (retrieve_golden_example for DB examples)
  </read_first>
  <action>
    Verify and improve high-quality example retrieval:

1. **Check get_best_examples implementation**:
   - Query must filter by overall_score >= 80
   - Must return final_code for RAG injection
   - Should include domain/topic for filtering

2. **Verify RAG integration**:
   - retrieve_golden_example calls get_best_examples
   - High-scorers are formatted as [OK] HIGH-SCORING EXAMPLE
   - Truncated examples still preserve useful patterns

3. **Test retrieval**:
   - With database containing high-scoring examples, verify retrieval works
   - Without database (db=None), should gracefully skip DB examples
   - Should return at least corpus patterns even without DB examples

4. **Enhance if needed**:
   - Ensure limit=3 is sufficient
   - Consider adding recency bias for newer high-scorers
   - Verify examples are truncated properly but patterns preserved
  </action>
  <acceptance_criteria>
    - get_best_examples returns examples with overall_score >= 80
    - retrieve_golden_example includes DB examples when available
    - Works gracefully when database unavailable
  </acceptance_criteria>
  <verify>
    <automated>python -c "from RAG.RAG_system import retrieve_golden_example; r = retrieve_golden_example('math', 'derivative', [], db=None); print('DB examples section present:', 'HIGH-SCORING' in r or 'PROVEN PATTERN' in r)"</automated>
  </verify>
  <done>High-quality examples (≥80 score) retrievable for RAG injection</done>
</task>

</tasks>

<verification>
- Verify evaluation triggers on successful render
- Check /stats returns enhanced metrics
- Test high-scorer retrieval with and without database
</verification>

<success_criteria>
- Every successful render produces a quality score (EVAL-01)
- Scores are visible via /stats endpoint (EVAL-03)
- High-quality examples are retrievable for RAG (via EVAL-01/02 infrastructure)
</success_criteria>

<output>
After completion, update `.planning/phases/04-evaluation-learning/04-01-PLAN-SUMMARY.md`
</output>
