---
phase: 03-voiceover-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - algorithms/tts.py
  - algorithms/request_analysis.py
  - app.py
autonomous: true
requirements:
  - VOIC-01
  - VOIC-02
  - VOIC-03
  - VOIC-04

must_haves:
  truths:
    - "User can enable voiceover and receive narrated video"
    - "Audio timing synchronizes correctly with animation"
    - "TTS generates natural-sounding narration"
    - "Fallback to silent video works when TTS fails"
  artifacts:
    - path: "algorithms/tts.py"
      provides: "TTS generation and audio/video merge"
      contains: "generate_voiceover"
    - path: "algorithms/request_analysis.py"
      provides: "Narrated plan creation with timing"
      contains: "create_narrated_plan"
    - path: "app.py"
      provides: "Voiceover pipeline wiring"
      contains: "voiceover"

must_verifs:
  - verify_voiceover_enabled: "voiceover=True triggers TTS pipeline"
  - verify_audio_sync: "Audio duration matches animation timing"
  - verify_fallback: "Silent video returned when TTS fails"
---

<objective>
Complete and validate the voiceover pipeline for Phase 3. Test audio-video sync, handle edge cases (empty segments, TTS failures), and ensure timing contract drives animation duration. Addresses VOIC-01, VOIC-02, VOIC-03, VOIC-04 requirements.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@algorithms/tts.py
@algorithms/request_analysis.py
@app.py

**Phase requirement IDs:** VOIC-01, VOIC-02, VOIC-03, VOIC-04

**Project context:**
- Voiceover infrastructure exists: TTS generation, audio/video merge, narrated plan creation
- Timing contract: audio duration should drive animation timing
- Parallel TTS generation is implemented
- ffmpeg merge is implemented
- Need to validate end-to-end and handle edge cases
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate timing contract in animation generation</name>
  <files>algorithms/ai_functions.py, algorithms/tts.py</files>
  <read_first>
    - algorithms/ai_functions.py (timing contract section around line 516)
    - algorithms/tts.py (generate_voiceover, merge_audio_video)
  </read_first>
  <action>
    Ensure audio duration from TTS properly drives animation timing:

1. **Check timing contract implementation**:
   - Look at how `segment_durations` from TTS is passed to `generate_manim_code()`
   - Verify the timing contract string is injected into the generation prompt
   - Check that generation respects pre-recorded audio durations

2. **Test timing synchronization**:
   - When voiceover=True, the system:
     a) Creates narrated plan with estimated durations
     b) Generates TTS audio for each segment
     c) Measures actual audio durations
     d) Passes durations to code generation as timing contract
     e) Code should use these durations for run_time

3. **Fix any timing contract issues**:
   - Ensure `segment_durations` dict format matches what's expected
   - Verify the timing contract string in system prompt is clear
   - Check that run_time values in generated code match audio durations

Current flow should be:
```
create_narrated_plan() → generate_voiceover() → get actual durations
→ pass to generate_manim_code(segment_durations=durations)
→ timing contract injected into prompt
→ generated code uses correct run_time values
```
  </action>
  <acceptance_criteria>
    - grep "TIMING CONTRACT" ai_functions.py shows timing contract injection
    - segment_durations passed correctly to generate_manim_code
    - Generated code uses run_time values from audio durations
  </acceptance_criteria>
  <verify>
    <automated>grep -c "TIMING CONTRACT" algorithms/ai_functions.py</automated>
  </verify>
  <done>Timing contract drives animation duration based on actual audio</done>
</task>

<task type="auto">
  <name>Task 2: Handle TTS failure edge cases</name>
  <files>algorithms/tts.py, app.py</files>
  <read_first>
    - algorithms/tts.py (generate_voiceover function)
    - app.py (save_and_render function around audio merge)
  </read_first>
  <action>
    Ensure graceful fallback when TTS fails:

1. **Check error handling in generate_voiceover()**:
   - When TTS API fails, should return error dict with fallback duration
   - Default fallback duration should be ~5 seconds per segment
   - Should not crash the pipeline

2. **Check merge_audio_video() fallback**:
   - When no audio segments available, should return original video
   - Check the code path: if audio_files is empty, return original video
   - Verify no crash when audio_segments is None or empty

3. **Test fallback scenarios**:
   - TTS API timeout → use estimated durations, continue with silent video
   - Empty narration text → skip segment, don't crash
   - Very long narration (>60s) → handle gracefully
   - ffmpeg not installed → should fail gracefully with clear error message

4. **Add retry logic for TTS**:
   - If first TTS attempt fails, retry once with exponential backoff
   - If retry fails, use fallback and log warning

Current code already has:
- `_generate_single_segment` catches exceptions and returns error dict
- `merge_audio_video` has fallback if no audio files

Enhancements needed:
- Add retry logic for transient TTS failures
- Add explicit handling for empty narration segments
- Improve error messages
  </action>
  <acceptance_criteria>
    - TTS failure returns fallback durations, doesn't crash
    - merge_audio_video handles empty/missing audio gracefully
    - Empty narration segments are skipped without error
    - ffprobe timeout handled with file-size estimation fallback
  </acceptance_criteria>
  <verify>
    <automated>python -c "from algorithms.tts import generate_voiceover; result = generate_voiceover([], '/tmp/test'); print('Empty segments handled:', result == {})"</automated>
  </verify>
  <done>TTS failures handled gracefully with fallback to silent video</done>
</task>

<task type="auto">
  <name>Task 3: Validate audio-video sync</name>
  <files>algorithms/tts.py, app.py</files>
  <read_first>
    - algorithms/tts.py (merge_audio_video function)
    - app.py (render flow around audio merge)
  </read_first>
  <action>
    Test and validate audio-video synchronization:

1. **Check ffmpeg merge logic**:
   - Verify ffmpeg command correctly concatenates audio segments
   - Verify ffmpeg command correctly merges audio with video
   - Check that output audio duration matches combined audio segments

2. **Test audio concatenation order**:
   - segment_order determines which audio file plays when
   - Verify ffmpeg concat demuxer is used correctly
   - Verify audio is properly aligned with video timeline

3. **Check timing accuracy**:
   - Audio should start at video start (0:00)
   - Each segment's audio should play during its corresponding animation
   - No audio overlap or gaps

4. **Add validation test**:
   - After merge, verify output video duration ≈ max(audio_duration, video_duration)
   - Verify audio file exists and has non-zero size
   - Log warning if significant duration mismatch

Current merge_audio_video implementation uses:
- ffmpeg -i video.mp4 -i audio.mp3 -c:v copy -c:a aac output.mp4
- Should work for basic case, needs edge case handling
  </action>
  <acceptance_criteria>
    - ffmpeg merge command correctly syncs audio to video
    - Audio starts at beginning of video
    - Combined audio duration matches expected total
    - Output video is playable (no codec errors)
  </acceptance_criteria>
  <verify>
    <automated>grep "ffmpeg" algorithms/tts.py | head -5</automated>
  </verify>
  <done>Audio-video sync validated with accurate segment timing</done>
</task>

<task type="auto">
  <name>Task 4: Add voice selection options</name>
  <files>config.py, algorithms/tts.py</files>
  <read_first>
    - config.py (TTS_MODEL, TTS_VOICE settings)
    - algorithms/tts.py (generate_segment_audio function)
  </read_first>
  <action>
    Add configurable voice selection for TTS:

1. **Check current voice config**:
   - TTS_MODEL and TTS_VOICE in config.py
   - Currently uses gpt-4o-mini-tts model
   - Need to support voice selection per request

2. **Add voice options**:
   - Available voices: alloy, echo, fable, onyx, nova, shimmer (OpenAI TTS voices)
   - Allow user to specify voice preference
   - Default to a natural, educational voice (nova recommended)

3. **Wire voice selection through pipeline**:
   - Accept voice parameter in /api/generate endpoint
   - Pass voice through to generate_voiceover()
   - Pass voice through to narrated plan creation

4. **Update config defaults**:
   - TTS_MODEL = "gpt-4o-mini-tts" (keep for cost efficiency)
   - TTS_VOICE = "nova" (natural, clear for education)

Add to /api/generate request:
```json
{"prompt": "...", "voiceover": true, "voice": "nova"}
```

Where voice is optional and defaults to config TTS_VOICE.
  </action>
  <acceptance_criteria>
    - Voice selection available via API parameter
    - Different voices work for TTS generation
    - Default voice is appropriate for educational content
    - Voice parameter is passed through the full pipeline
  </acceptance_criteria>
  <verify>
    <automated>grep "TTS_VOICE" config.py</automated>
  </verify>
  <done>Voice selection configurable with educational defaults</done>
</task>

</tasks>

<verification>
- Test full voiceover pipeline with real TTS API
- Verify audio plays at correct times in merged video
- Test fallback when TTS fails
- Validate timing contract with multiple segments
</verification>

<success_criteria>
- User can enable voiceover and receive narrated video (VOIC-01)
- Audio timing synchronizes correctly with animation (VOIC-03)
- TTS generates natural narration (VOIC-02)
- Fallback to silent video works when TTS fails (VOIC-03)
- Audio duration drives animation timing (VOIC-04)
</success_criteria>

<output>
After completion, create `.planning/phases/03-voiceover-polish/03-01-PLAN-SUMMARY.md`
</output>
