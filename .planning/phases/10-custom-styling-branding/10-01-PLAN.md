---
phase: 10-custom-styling-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nima-frontend/src/app/layout.tsx
  - nima-frontend/src/app/page.tsx
  - nima-frontend/src/components/ThemeProvider.tsx
  - nima-frontend/src/styles/globals.css
autonomous: true
requirements:
  - M3-STYLE-01
  - M3-STYLE-04

must_haves:
  truths:
    - "User can switch between light and dark themes"
    - "User can select custom accent colors"
    - "Theme preference persists across sessions"
    - "Color palette can be overridden per-video"
  artifacts:
    - path: "nima-frontend/src/components/ThemeProvider.tsx"
      provides: "Theme context and toggle logic"
      min_lines: 50
    - path: "nima-frontend/src/styles/globals.css"
      provides: "CSS custom properties for theming"
      contains: "--accent-color"
    - path: "nima-frontend/src/app/layout.tsx"
      provides: "Root layout with theme provider"
      exports: ["ThemeProvider"]
  key_links:
    - from: "nima-frontend/src/app/layout.tsx"
      to: "ThemeProvider.tsx"
      via: "import and wrap children"
    - from: "ThemeProvider.tsx"
      to: "globals.css"
      via: "CSS custom properties"
---

<objective>
Build the theme system and custom color palette support for the NIMA frontend.
</objective>

<context>
@.planning/REQUIREMENTS.md (M3-STYLE-01, M3-STYLE-04)
@nima-frontend/package.json (Next.js 16, React 19, Tailwind CSS 4)
@nima-frontend/src/app/layout.tsx (root layout)
@nima-frontend/src/styles/globals.css (global styles)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ThemeProvider component</name>
  <files>nima-frontend/src/components/ThemeProvider.tsx</files>
  <action>
    Create ThemeProvider component using React Context for theme management.
    
    Requirements:
    - Create ThemeContext with { theme, setTheme, accentColor, setAccentColor }
    - Support themes: 'light', 'dark', 'custom'
    - Store preferences in localStorage under 'nima-theme' and 'nima-accent-color'
    - Apply theme class to document root (document.documentElement.classList)
    - CSS custom properties for accent color: --accent-color, --accent-color-foreground
    - Default accent: #8B5CF6 (violet)
    
    Theme colors (CSS variables):
    - light: --bg-primary: #ffffff, --bg-secondary: #f3f4f6, --text-primary: #111827
    - dark: --bg-primary: #0f172a, --bg-secondary: #1e293b, --text-primary: #f9fafb
    
    Export: ThemeProvider, useTheme hook
  </action>
  <verify>
    <automated>test -f nima-frontend/src/components/ThemeProvider.tsx && echo "EXISTS"</automated>
  </verify>
  <done>ThemeProvider.tsx exports ThemeProvider and useTheme with localStorage persistence</done>
</task>

<task type="auto">
  <name>Task 2: Update global CSS with theme variables</name>
  <files>nima-frontend/src/styles/globals.css</files>
  <action>
    Add CSS custom properties for theming to globals.css.
    
    Add at root level (using :root for defaults):
    ```
    :root {
      --accent-color: #8B5CF6;
      --accent-color-foreground: #ffffff;
      /* Light theme defaults */
      --bg-primary: #ffffff;
      --bg-secondary: #f3f4f6;
      --text-primary: #111827;
      --text-secondary: #6b7280;
    }
    
    .dark {
      --bg-primary: #0f172a;
      --bg-secondary: #1e293b;
      --text-primary: #f9fafb;
      --text-secondary: #9ca3af;
    }
    ```
    
    Also add:
    - Theme transition: * { transition: background-color 0.2s, color 0.2s }
    - Accent color utility: .accent { color: var(--accent-color); background: var(--accent-color); }
  </action>
  <verify>
    <automated>grep -c "accent-color" nima-frontend/src/styles/globals.css && grep -c "dark" nima-frontend/src/styles/globals.css</automated>
  </verify>
  <done>globals.css contains CSS custom properties for light/dark themes and accent color</done>
</task>

<task type="auto">
  <name>Task 3: Wrap app with ThemeProvider</name>
  <files>nima-frontend/src/app/layout.tsx</files>
  <action>
    Modify layout.tsx to wrap children with ThemeProvider.
    
    Current imports should include ThemeProvider.
    Wrap {children} with <ThemeProvider>...</ThemeProvider>.
    Keep existing html lang="en" and metadata.
  </action>
  <verify>
    <automated>grep -c "ThemeProvider" nima-frontend/src/app/layout.tsx</automated>
  </verify>
  <done>layout.tsx wraps application with ThemeProvider, theme applies to entire app</done>
</task>

<task type="auto">
  <name>Task 4: Add theme toggle UI to main page</name>
  <files>nima-frontend/src/app/page.tsx</files>
  <action>
    Add theme toggle controls to the main page.
    
    Add to the header/sidebar area:
    - Light/Dark toggle button (sun/moon icons via Unicode: ☀ / ☾)
    - Accent color picker: 4 preset colors (#8B5CF6 violet, #3B82F6 blue, #10B981 green, #F59E0B amber)
    
    Use useTheme hook:
    ```tsx
    const { theme, setTheme, accentColor, setAccentColor } = useTheme();
    ```
    
    When accent color changes, update document CSS variable:
    document.documentElement.style.setProperty('--accent-color', newColor)
  </action>
  <verify>
    <automated>grep -c "useTheme" nima-frontend/src/app/page.tsx</automated>
  </verify>
  <done>page.tsx has working theme toggle and accent color picker</done>
</task>

</tasks>

<verification>
1. npm run build in nima-frontend succeeds
2. Theme toggle switches between light/dark class on html element
3. Accent color picker changes --accent-color CSS variable
4. Theme persists after page refresh (localStorage check)
</verification>

<success_criteria>
User can toggle between light and dark themes. User can select from preset accent colors. Theme preference persists across browser sessions. Custom accent color is applied site-wide.
</success_criteria>

<output>
After completion, create `.planning/phases/10-custom-styling-branding/10-01-PLAN-SUMMARY.md`
</output>
