---
phase: 02-quality-reliability
plan: 03
type: execute
wave: 1
depends_on:
  - 02-02
files_modified:
  - RAG/RAG_system.py
  - algorithms/ai_functions.py
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03

must_haves:
  truths:
    - "Golden examples are retrieved based on domain/topic/subtopic matching"
    - "High-quality past examples (≥80 score) are retrievable from database"
    - "Error patterns inform future generation to avoid repeated mistakes"
  artifacts:
    - path: "RAG/RAG_system.py"
      provides: "RAG retrieval with semantic matching"
      contains: "retrieve_golden_example"
    - path: "RAG/RAG_system.py"
      provides: "Error pattern integration"
      contains: "error_patterns"
    - path: "app.py"
      provides: "High-scorer retrieval from DB"
      contains: "get_best_examples"

must_verifs:
  - verify_rag_retrieval: "Domain/topic matching returns relevant patterns"
  - verify_high_scorers: "High-quality examples (≥80) are retrievable"
  - verify_error_feedback: "Error patterns are injected into generation prompts"
---

<objective>
Improve RAG system to better retrieve golden examples, high-scoring past examples, and error patterns. Current RAG uses basic keyword matching and doesn't integrate error feedback loop. Addresses RAG-01, RAG-02, RAG-03 requirements.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@RAG/RAG_system.py
@algorithms/ai_functions.py
@app.py

**Phase requirement IDs:** RAG-01, RAG-02, RAG-03

**Project context:**
- NIMA RAG uses simple keyword matching (+1 per token, +5 for phrase bonus)
- High-scoring examples (≥80) are retrieved from DB but only 2 at a time
- Error patterns exist in DB but are NOT integrated into RAG retrieval
- No semantic similarity/embeddings for better matching
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve RAG retrieval scoring with semantic signal</name>
  <files>RAG/RAG_system.py</files>
  <read_first>
    - RAG/RAG_system.py (retrieve_patterns and retrieve_golden_example functions)
  </read_first>
  <action>
    Enhance the retrieval scoring in RAG/RAG_system.py retrieve_patterns():

1. **Improve scoring algorithm** in retrieve_patterns():
   - Add TF-IDF style weighting (rare tags that match are more valuable)
   - Add domain boost: exact domain match adds +3 bonus
   - Add recency signal: newer patterns in corpus get slight boost
   - Consider subtopic overlap more heavily (currently underweighted)

2. **Add fallback chain**:
   - Try exact domain+topic match first
   - Fall back to domain-only match if no topic matches
   - Fall back to related domain patterns
   - Last resort: return highest-frequency general patterns

3. **Add diversity bonus**:
   - Don't return 3 nearly identical patterns
   - Penalize patterns with high tag overlap in scoring

Current scoring:
  - +1 per keyword match
  - +5 for multi-word phrase match

Improved scoring should be:
  - +1 per keyword match
  - +3 for exact domain match on tag
  - +5 for multi-word phrase match
  - +2 per matching subtopic
  - -2 penalty for patterns sharing most tags with higher-scored pattern
  </action>
  <acceptance_criteria>
    - grep "TF-IDF\|tfidf\|semantic\|similarity" RAG_system.py returns matches OR improved scoring documented
    - retrieve_patterns returns diverse patterns (low tag overlap)
    - retrieve_patterns fallback chain works (domain → related domain → general)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from RAG.RAG_system import retrieve_patterns; p = retrieve_patterns('math', 'derivative', ('calculus', 'limit'), 3); print(len(p)); print([pattern['notes'] for pattern in p])"</automated>
  </verify>
  <done>RAG retrieval uses improved scoring with semantic signals and fallback chain</done>
</task>

<task type="auto">
  <name>Task 2: Expand high-quality example retrieval from database</name>
  <files>RAG/RAG_system.py, app.py</files>
  <read_first>
    - RAG/RAG_system.py (retrieve_golden_example function)
    - app.py (get_best_examples function)
  </read_first>
  <action>
    Improve high-scoring example retrieval in RAG/RAG_system.py and app.py:

1. **Increase retrieval limit** in retrieve_golden_example():
   - Change from limit=2 to limit=3 for DB examples
   - Ensure at least 1 DB example is included when available
   - Truncate long examples but preserve key patterns

2. **Improve get_best_examples()** in app.py:
   - Add topic/subtopic matching (currently only domain filter)
   - Add diversity: don't return 3 examples that are too similar
   - Consider recency: prefer newer high-scoring examples
   - Add score threshold tuning: 80 is baseline, aim for 85+ when available

3. **Add example quality metadata** to retrieval:
   - Include overall_score in formatted output
   - Show what makes the example high-quality (educational value, visual quality)
   - Add tags showing what domain/topic this example covers

Current query:
```sql
WHERE rj.status = 'done' AND ae.overall_score >= 80
ORDER BY ae.overall_score DESC LIMIT 2
```

Improved query should:
```sql
WHERE rj.status = 'done' AND ae.overall_score >= 80
AND (r.domain = %s OR r.topic SIMILAR TO %s)
ORDER BY ae.overall_score DESC, r.created_at DESC
LIMIT 3
```

4. **Add format_best_example() helper** to RAG_system.py:
   - Format DB examples consistently with corpus patterns
   - Truncate to 1000 chars but preserve structure
   - Add header showing score and domain/topic
  </action>
  <acceptance_criteria>
    - retrieve_golden_example returns at least 3 total patterns (DB + corpus combined)
    - get_best_examples uses topic matching in addition to domain
    - DB examples include quality score in formatted output
    - High-scorer retrieval gets examples with 85+ score when available
  </acceptance_criteria>
  <verify>
    <automated>python -c "from RAG.RAG_system import retrieve_golden_example; result = retrieve_golden_example('math', 'derivative', ['calculus'], db=None); print('DB examples section found:', '[OK] HIGH-SCORING EXAMPLE' in result or '[OK] PROVEN PATTERN' in result)"</automated>
  </verify>
  <done>High-quality example retrieval expanded with topic matching and diversity</done>
</task>

<task type="auto">
  <name>Task 3: Integrate error patterns into RAG retrieval loop</name>
  <files>RAG/RAG_system.py, algorithms/ai_functions.py</files>
  <read_first>
    - RAG/RAG_system.py (retrieve_golden_example function)
    - algorithms/ai_functions.py (get_error_warnings function, generate_manim_code call chain)
    - app.py (get_error_patterns function)
  </read_first>
  <action>
    Integrate error pattern avoidance into the RAG-driven generation pipeline:

1. **Create error-aware retrieval function** in RAG/RAG_system.py:
   ```python
   def retrieve_with_error_avoidance(domain, topic, subtopics, db, limit=3):
       """Retrieve patterns while avoiding known error patterns."""
       # Get base patterns
       patterns = retrieve_golden_example(domain, topic, subtopics, db)
       # Get error patterns to avoid
       if db and db.available:
           errors = db.get_error_patterns(limit=5)
           # Inject error avoidance guidance into prompt section
           error_section = format_error_avoidance(errors)
           patterns += "\n\n" + error_section
       return patterns
   ```

2. **Create format_error_avoidance() helper** in RAG/RAG_system.py:
   - Format top recurring errors as avoidance guidance
   - Group by error_category
   - Show fix_description as "how to avoid" guidance
   - Limit to 3 most common unresolved errors

3. **Wire into generation pipeline**:
   - Option A: Modify retrieve_golden_example to always include error patterns
   - Option B: Create new retrieve_with_error_avoidance and use it in generate_manim_code
   - Recommend Option A for simplicity (error patterns are always relevant)

4. **Update generate_manim_code()** in ai_functions.py:
   - Change from: `golden_examples = retrieve_golden_example(domain, topic, subtopics, db)`
   - To: `golden_examples = retrieve_with_error_avoidance(domain, topic, subtopics, db)`
   - OR ensure get_error_warnings() is called and results are injected

5. **Add error pattern recording trigger**:
   - Ensure save_render_error() calls db.record_error_pattern() for new errors
   - Verify error_patterns table is populated over time
  </action>
  <acceptance_criteria>
    - retrieve_golden_example includes error avoidance guidance when db available
    - grep "error_patterns\|get_error_patterns" RAG_system.py returns matches
    - grep "avoid\|ERROR\|error" RAG_system.py formatted output includes error guidance
    - Error patterns from DB are injected into generation prompts
  </acceptance_criteria>
  <verify>
    <automated>python -c "from RAG.RAG_system import retrieve_golden_example; result = retrieve_golden_example('math', 'derivative', ['calculus'], db=None); print('Error guidance present:', 'avoid' in result.lower() or 'fix' in result.lower())"</automated>
  </verify>
  <done>Error patterns inform generation to avoid repeated mistakes (RAG-03)</done>
</task>

</tasks>

<verification>
- Test retrieval with various domain/topic combinations
- Verify DB examples appear when database is available
- Check error patterns are formatted and injected into prompts
- Confirm error recording happens on render failures
</verification>

<success_criteria>
- Golden examples retrieved based on domain/topic/subtopic matching (RAG-01)
- High-quality past examples (≥80 score) retrievable from database (RAG-02)
- Error patterns inform future generation to avoid repeated mistakes (RAG-03)
- RAG retrieval is diverse and uses improved scoring
- Error avoidance guidance appears in generation prompts
</success_criteria>

<output>
After completion, update `.planning/phases/02-quality-reliability/02-01-PLAN-SUMMARY.md`
</output>
