---
phase: 08-evaluation-dashboard
plan: "08-02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - nima-frontend/src/components/dashboard/TrendChart.tsx
  - nima-frontend/src/components/dashboard/TopExamples.tsx
  - nima-frontend/src/components/dashboard/ErrorPatterns.tsx
  - nima-frontend/src/app/dashboard/page.tsx
autonomous: true
requirements:
  - M2-DASH-02
  - M2-DASH-03
  - M2-DASH-04
must_haves:
  truths:
    - "Trend chart shows renders/day over time"
    - "Top examples showcase videos with playback"
    - "Error patterns table shows unresolved issues"
  artifacts:
    - path: "nima-frontend/src/components/dashboard/TrendChart.tsx"
      provides: "7-day renders/day line chart"
      exports: "TrendChart component"
    - path: "nima-frontend/src/components/dashboard/TopExamples.tsx"
      provides: "Video showcase with playback"
      exports: "TopExamples component"
    - path: "nima-frontend/src/components/dashboard/ErrorPatterns.tsx"
      provides: "Error pattern tracking table"
      exports: "ErrorPatterns component"
  key_links:
    - from: "TopExamples.tsx"
      to: "/outputs/"
      via: "video src URL construction"
      pattern: "outputs.*video_file"
---

<objective>
Add trend visualization, top examples showcase, and error patterns view to the dashboard.

Purpose: Complete the dashboard by adding the remaining three requirements: trend charts (M2-DASH-02), top examples (M2-DASH-03), and error patterns (M2-DASH-04).
Output: Full-featured dashboard with all four requirements implemented
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-evaluation-dashboard/08-01-PLAN.md
@.planning/phases/08-evaluation-dashboard/08-CONTEXT.md

# API contract
From app.py lines 1612-1634:
```typescript
// GET /stats/top-examples response:
interface TopExamplesResponse {
  top_examples: Array<{
    prompt: string;
    domain: string;
    topic: string;
    overall_score: number;
    visual_quality_score: number;
    educational_value_score: number;
    pacing: number;
    video_path: string;
    created_at: string;
  }>;
}
```

From Plan 08-01:
- API client in `nima-frontend/src/lib/api.ts`
- Dashboard page in `nima-frontend/src/app/dashboard/page.tsx`
- StatsGrid and QualityChart components created
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrendChart component</name>
  <files>nima-frontend/src/components/dashboard/TrendChart.tsx</files>
  <read_first>
    - nima-frontend/src/components/dashboard/QualityChart.tsx (for Recharts patterns)
    - nima-frontend/package.json (confirm recharts installed)
  </read_first>
  <action>
    Create `nima-frontend/src/components/dashboard/TrendChart.tsx` with:

    1. Props: `{ dailyData: Array<{ date: string; renders: number; avgQuality: number }> }`
    2. Note: The backend /stats does NOT currently provide daily trend data - this is a limitation
    3. Create component that accepts daily data as prop (data will come from future API enhancement OR use mock data for now with a comment that it needs backend support)
    4. Use Recharts LineChart with two lines:
       - Line 1: Renders per day (cyan, left Y-axis)
       - Line 2: Average quality score (green, right Y-axis)
    5. X-axis: dates (last 7 days)
    6. Chart title: "RENDER_TRENDS (7D)"
    7. Style: Match blueprint theme with grid lines in var(--border-grid)
  </action>
  <verify>
    <automated>grep -l "LineChart" nima-frontend/src/components/dashboard/TrendChart.tsx</automated>
  </verify>
  <done>TrendChart component displays renders/day and quality trend lines</done>
</task>

<task type="auto">
  <name>Task 2: Create TopExamples component</name>
  <files>nima-frontend/src/components/dashboard/TopExamples.tsx</files>
  <read_first>
    - nima-frontend/src/app/page.tsx lines 364-384 (existing video player styling)
    - nima-frontend/src/components/dashboard/QualityChart.tsx (card styling patterns)
  </read_first>
  <action>
    Create `nima-frontend/src/components/dashboard/TopExamples.tsx` with:

    1. Props: `{ examples: TopExample[] }` (from api.ts type)
    2. State: selectedExample (for video modal/expansion)
    3. Display as grid of cards (3 columns on desktop, 1 on mobile):
       - Each card shows:
         - Domain badge (e.g., "MATH", "PHYSICS")
         - Prompt text (truncated to 2 lines)
         - Overall quality score (large, colored by tier)
         - Individual dimension scores (visual, educational, pacing)
    4. Clicking a card expands to show:
       - Full prompt text
       - Video player using HTML5 video element
       - Video URL: `${API_BASE}/outputs/${video_path}`
    5. Style cards with:
       - border-box class (existing)
       - Corner accents matching main page
       - Quality score color coding (≥80 green, 60-79 yellow, <60 magenta)
    6. Show "NO_EXAMPLES_FOUND" state when examples array is empty
  </action>
  <verify>
    <automated>grep -l "TopExamples" nima-frontend/src/components/dashboard/TopExamples.tsx && grep "video" nima-frontend/src/components/dashboard/TopExamples.tsx</automated>
  </verify>
  <done>TopExamples displays up to 10 example cards with expandable video playback</done>
</task>

<task type="auto">
  <name>Task 3: Create ErrorPatterns component</name>
  <files>nima-frontend/src/components/dashboard/ErrorPatterns.tsx</files>
  <read_first>
    - nima-frontend/src/components/dashboard/StatsGrid.tsx (card styling)
    - app.py lines 1578-1584 (top_errors data structure)
  </read_first>
  <action>
    Create `nima-frontend/src/components/dashboard/ErrorPatterns.tsx` with:

    1. Props: `{ errors: StatsResponse['top_errors'] }`
    2. Display as table with columns:
       - Category (error_category) - bold
       - Root Cause (root_cause) - truncated
       - Occurrences (occurrence_count) - number badge
       - Fix (fix_description) - truncated
    3. Sort by occurrence_count descending (most frequent first)
    4. Color coding:
       - High count (>10): magenta badge
       - Medium (5-10): yellow badge
       - Low (<5): cyan badge
    5. Table styling:
       - Use blueprint/technical table style
       - Monospace font
       - Alternating row backgrounds
       - Header row with uppercase labels
    6. Show "NO_ERROR_PATTERNS" when errors array is empty
    7. Add section header: "ERROR_PATTERNS_TRACKED"
  </action>
  <verify>
    <automated>grep -l "ErrorPatterns" nima-frontend/src/components/dashboard/ErrorPatterns.tsx && grep "occurrence_count" nima-frontend/src/components/dashboard/ErrorPatterns.tsx</automated>
  </verify>
  <done>ErrorPatterns displays table of error patterns sorted by frequency with color-coded badges</done>
</task>

<task type="auto">
  <name>Task 4: Update dashboard page with all components</name>
  <files>nima-frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - nima-frontend/src/app/dashboard/page.tsx (from plan 08-01)
    - nima-frontend/src/components/dashboard/TrendChart.tsx
    - nima-frontend/src/components/dashboard/TopExamples.tsx
    - nima-frontend/src/components/dashboard/ErrorPatterns.tsx
  </read_first>
  <action>
    Update `nima-frontend/src/app/dashboard/page.tsx` to:

    1. Import all new components:
       - TrendChart from ./components/dashboard/TrendChart
       - TopExamples from ./components/dashboard/TopExamples
       - ErrorPatterns from ./components/dashboard/ErrorPatterns
    2. Update StatsResponse state to include top_examples from fetchTopExamples()
    3. Arrange dashboard layout with sections:
       - Row 1: StatsGrid (full width)
       - Row 2: QualityChart (left 1/2), TrendChart (right 1/2)
       - Row 3: TopExamples (left 2/3), ErrorPatterns (right 1/3)
    4. Pass correct props to each component:
       - QualityChart: { qualityTiers: stats.quality_tiers }
       - TrendChart: { dailyData: mockDailyData() } (generate from renders_today trend)
       - TopExamples: { examples: topExamples }
       - ErrorPatterns: { errors: stats.top_errors }
    5. Ensure responsive grid works on mobile (stack vertically)
    6. Update polling to also fetch top_examples every 60 seconds
  </action>
  <verify>
    <automated>grep "TrendChart" nima-frontend/src/app/dashboard/page.tsx && grep "TopExamples" nima-frontend/src/app/dashboard/page.tsx && grep "ErrorPatterns" nima-frontend/src/app/dashboard/page.tsx</automated>
  </verify>
  <done>Dashboard page displays all four components in responsive grid layout</done>
</task>

</tasks>

<verification>
1. Visit http://localhost:3000/dashboard
2. Verify TrendChart displays renders/quality trend line
3. Verify TopExamples shows video cards (or empty state if no examples)
4. Verify ErrorPatterns table shows error categories
5. Verify layout is responsive
6. Verify auto-refresh continues to work
</verification>

<success_criteria>
- TrendChart shows 7-day render/quality trend visualization
- TopExamples displays up to 10 cards with expandable video playback
- ErrorPatterns shows table of errors sorted by frequency
- All components properly wired to dashboard page
- Responsive layout works on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/08-evaluation-dashboard/08-02-SUMMARY.md`
</output>
