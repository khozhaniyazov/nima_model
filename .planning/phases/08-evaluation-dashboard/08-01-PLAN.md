---
phase: 08-evaluation-dashboard
plan: "08-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - nima-frontend/src/app/dashboard/page.tsx
  - nima-frontend/src/components/dashboard/StatsGrid.tsx
  - nima-frontend/src/components/dashboard/QualityChart.tsx
  - nima-frontend/src/lib/api.ts
autonomous: true
requirements:
  - M2-DASH-01
  - M2-DASH-02
must_haves:
  truths:
    - "Dashboard displays at /dashboard route"
    - "Quality metrics cards show current stats"
    - "Quality distribution chart visualizes score tiers"
  artifacts:
    - path: "nima-frontend/src/app/dashboard/page.tsx"
      provides: "Main dashboard page with grid layout"
      min_lines: 50
    - path: "nima-frontend/src/components/dashboard/QualityChart.tsx"
      provides: "Quality tier distribution pie/bar chart"
      exports: "QualityChart component"
    - path: "nima-frontend/src/lib/api.ts"
      provides: "TypeScript API client for /stats endpoint"
      exports: "fetchStats, StatsResponse type"
  key_links:
    - from: "dashboard/page.tsx"
      to: "/stats"
      via: "fetch in useEffect"
      pattern: "fetch.*stats"
    - from: "QualityChart.tsx"
      to: "StatsGrid.tsx"
      via: "data prop"
      pattern: "quality_tiers"
---

<objective>
Build the Evaluation Dashboard foundation with quality metrics display and distribution chart.

Purpose: Establish the dashboard structure, API integration, and core quality visualization that subsequent plans extend.
Output: Dashboard page at /dashboard with stats cards and quality distribution chart
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-evaluation-dashboard/08-CONTEXT.md
@.planning/phases/08-evaluation-dashboard/08-RESEARCH.md

# API contract from existing backend
From app.py (lines 1530-1607):
```typescript
// GET /stats response shape:
interface StatsResponse {
  stats: {
    total_requests: number;
    successful_renders: number;
    avg_quality_score: number | null;
    unique_error_patterns: number;
    success_rate: number;
  };
  quality_dims: {
    avg_layout: number;
    avg_educational: number;
    avg_technical: number;
    avg_pacing: number;
    avg_manim: number;
  };
  quality_tiers: {
    pct_80_plus: number;
    pct_70_79: number;
    pct_60_69: number;
    pct_below_60: number;
  };
  top_domains: Array<{ domain: string; count: number }>;
  top_errors: Array<{
    error_category: string;
    root_cause: string;
    fix_description: string;
    occurrence_count: number;
  }>;
  renders_today: number;
  avg_render_duration: number;
  last_render_at: string;
}
```

From page.tsx (existing patterns):
```typescript
const API_BASE = "http://localhost:5000";
// Video URL pattern: ${API_BASE}/outputs/${video_file}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard API client</name>
  <files>nima-frontend/src/lib/api.ts</files>
  <read_first>
    - nima-frontend/src/app/page.tsx (API usage pattern)
    - app.py lines 1530-1607 (/stats endpoint implementation)
  </read_first>
  <action>
    Create TypeScript API client for the dashboard:

    1. Create `nima-frontend/src/lib/api.ts`
    2. Export interface `StatsResponse` matching the backend response shape above
    3. Export async function `fetchStats(): Promise<StatsResponse>` that:
       - Calls GET `${API_BASE}/stats`
       - Returns parsed JSON
       - Handles errors gracefully (returns empty stats on failure)
    4. Export async function `fetchTopExamples(): Promise<TopExample[]>` that:
       - Calls GET `${API_BASE}/stats/top-examples`
       - Returns `{ top_examples: TopExample[] }`
       - TopExample has: prompt, domain, topic, overall_score, visual_quality_score, educational_value_score, pacing, video_path, created_at
  </action>
  <verify>
    <automated>grep -l "fetchStats" nima-frontend/src/lib/api.ts && grep -l "StatsResponse" nima-frontend/src/lib/api.ts</automated>
  </verify>
  <done>API client exports fetchStats and fetchTopExamples with proper TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Create StatsGrid component</name>
  <files>nima-frontend/src/components/dashboard/StatsGrid.tsx</files>
  <read_first>
    - nima-frontend/src/app/page.tsx lines 207-232 (existing data-grid styling)
    - nima-frontend/src/app/globals.css (CSS variables)
  </read_first>
  <action>
    Create `nima-frontend/src/components/dashboard/StatsGrid.tsx` with:

    1. Props: `{ stats: StatsResponse['stats'], rendersToday: number, lastRenderAt: string }`
    2. Display 4-6 stat cards in a responsive grid:
       - Total Requests (total_requests)
       - Successful Renders (successful_renders) 
       - Success Rate (success_rate as percentage)
       - Avg Quality Score (avg_quality_score)
       - Renders Today (renders_today)
       - Unique Error Patterns (unique_error_patterns)
    3. Use existing blueprint/technical styling from page.tsx data-grid pattern:
       - Use CSS classes: data-grid, data-cell
       - Use colors: var(--accent-cyan), var(--accent-green), var(--accent-magenta)
       - Monospace font for numbers
    4. Color code values:
       - Quality ≥80: green
       - Quality 60-79: yellow
       - Quality <60: magenta
       - Error patterns: magenta
  </action>
  <verify>
    <automated>grep -l "StatsGrid" nima-frontend/src/components/dashboard/StatsGrid.tsx</automated>
  </verify>
  <done>StatsGrid component displays 6 metric cards with proper styling and color coding</done>
</task>

<task type="auto">
  <name>Task 3: Create QualityChart component</name>
  <files>nima-frontend/src/components/dashboard/QualityChart.tsx</files>
  <read_first>
    - nima-frontend/package.json (check existing deps)
  </read_first>
  <action>
    Create `nima-frontend/src/components/dashboard/QualityChart.tsx` with:

    1. Props: `{ qualityTiers: StatsResponse['quality_tiers'] }`
    2. Install recharts: `cd nima-frontend && npm install recharts`
    3. Create a horizontal bar chart showing quality tier distribution:
       - Bar 1: ≥80% (green) - label: "Excellent (≥80)"
       - Bar 2: 70-79% (blue/cyan) - label: "Good (70-79)"
       - Bar 3: 60-69% (yellow) - label: "Fair (60-69)"
       - Bar 4: <60% (magenta) - label: "Needs Work (<60)"
    4. Use Recharts BarChart with custom colors matching the blueprint theme
    5. Add percentage labels on bars
    6. Chart title: "QUALITY_DISTRIBUTION"
  </action>
  <verify>
    <automated>grep -l "BarChart" nima-frontend/src/components/dashboard/QualityChart.tsx && grep "recharts" nima-frontend/package.json</automated>
  </verify>
  <done>QualityChart displays horizontal bar chart of quality tier distribution with proper colors</done>
</task>

<task type="auto">
  <name>Task 4: Create main dashboard page</name>
  <files>nima-frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - nima-frontend/src/app/page.tsx (layout patterns, Corners component, WireframeLoader)
    - nima-frontend/src/app/globals.css (blueprint styling)
  </read_first>
  <action>
    Create `nima-frontend/src/app/dashboard/page.tsx` with:

    1. "use client" directive
    2. Imports: useState, useEffect from react
    3. State: stats (StatsResponse), loading (boolean), error (string)
    4. useEffect: call fetchStats() on mount, set loading during fetch
    5. Layout:
       - Blueprint grid background (copy from page.tsx)
       - Header: "EVALUATION_DASHBOARD" with NIMA branding
       - Main content area with:
         - StatsGrid component
         - QualityChart component
         - "Loading..." or error state handled
    6. Copy Corners component and styling patterns from page.tsx
    7. Include scanline overlay and crosshair effects for consistency
    8. Poll /stats every 60 seconds using setInterval in useEffect
  </action>
  <verify>
    <automated>grep -l "EvaluationDashboard" nima-frontend/src/app/dashboard/page.tsx && grep "fetchStats" nima-frontend/src/app/dashboard/page.tsx</automated>
  </verify>
  <done>Dashboard page loads at /dashboard, fetches stats, and displays StatsGrid and QualityChart components</done>
</task>

</tasks>

<verification>
1. Visit http://localhost:3000/dashboard
2. Verify header shows "EVALUATION_DASHBOARD"
3. Verify 6 stat cards display with values
4. Verify quality distribution bar chart renders
5. Verify no console errors
</verification>

<success_criteria>
- Dashboard page renders at /dashboard
- StatsGrid shows all 6 metrics with correct values from API
- QualityChart displays 4-bar distribution matching quality_tiers data
- Blueprint/technical styling consistent with main page
- Auto-refreshes every 60 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/08-evaluation-dashboard/08-01-SUMMARY.md`
</output>
